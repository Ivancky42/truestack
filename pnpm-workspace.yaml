# Single-package workspace. allowBuilds needs pnpm ≥ 10.26 (see packageManager).
packages:
  - "."

# Allow lifecycle scripts for native deps (Next/sharp, eslint tooling).
allowBuilds:
  sharp: true
  unrs-resolver: true
